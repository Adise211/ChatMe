import ChatMe from "@/components/ChatMe";

// TESTING ONLY!!!
const HomePage = () => {
  return (
    <>
      <ChatMe
        contacts={[]}
        conversations={[]}
        messages={[]}
        onInit={() => {}}
        conversationId={""}
      />
    </>
  );
};

export default HomePage;
